node_modules/
.expo/
.expo-shared/
dist/
build/
build-artifacts/
web-build/
.secrets/
android/
ios/
supabase/.temp/
.claude/
package-lock.json
pnpm-lock.yaml
yarn.lock
expo-env.d.ts

# Generated by release-please from the commit history; its formatting is the bot's,
# not Prettier's, so leave it out of the format gate (otherwise every release PR
# fails `verify` on the changelog and can never auto-merge).
CHANGELOG.md

# Firebase config files are downloaded from the Firebase console; keep them verbatim.
google-services.json
google-services.dev.json
