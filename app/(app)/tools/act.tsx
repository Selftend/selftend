import { ToolPlaceholderScreen } from "@/src/features/tools/tool-placeholder-screen";

export default function ActScreen() {
  return (
    <ToolPlaceholderScreen
      title="ACT"
      description="A future acceptance and commitment tool area. It will stay separate from CBT until its scope is reviewed."
    />
  );
}
