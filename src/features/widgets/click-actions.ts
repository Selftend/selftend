/** Single click action for every clickable; the handler opens clickActionData.path. */
export const OPEN_PATH = "OPEN_PATH";
/** Path used by the neutral tile to open the app's home. */
export const OPEN_APP_PATH = "/";
