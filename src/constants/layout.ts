// Below this width, step indicators (wizard-screen, step-pills) collapse to a
// single summary row instead of wrapping every step label across ~4 rows.
export const NARROW_STEP_INDICATOR_BREAKPOINT = 640;
